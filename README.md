# 📸 Photo Route Mapper

Українська версія | [Русская версия](README.ru.md)

> **Live Demo:** [zenzoik.fun](https://zenzoik.fun)

Вебзастосунок для автоматичного побудови інтерактивних маршрутів із фотографій з GPS-даними. Завантажуйте фото з геолокацією — система автоматично витягне координати з EXIF і побудує ваш маршрут на карті.

[![Live Demo](https://img.shields.io/badge/Live%20Demo-zenzoik.fun-brightgreen)](https://zenzoik.fun)
![Python](https://img.shields.io/badge/python-v3.11+-blue.svg)
![FastAPI](https://img.shields.io/badge/FastAPI-0.104+-green.svg)
![PostgreSQL](https://img.shields.io/badge/PostgreSQL-15-blue.svg)
![Docker](https://img.shields.io/badge/Docker-ready-blue.svg)
![License](https://img.shields.io/badge/license-MIT-blue.svg)

## 🚀 Швидкий старт

### Попередні вимоги

- [Docker](https://docs.docker.com/get-docker/) та [Docker Compose](https://docs.docker.com/compose/install/)
- Git

### Встановлення та запуск

```bash
# 1. Клонування репозиторію
git clone https://github.com/Zenzoik/photo-route-api.git
cd photo-route-api

# 2. Налаштування середовища
cp .env.example .env

# 3. Запуск застосунку
docker-compose up -d

# 4. Перевірка працездатності
curl http://localhost:8080/
```

**🎉 Готово!** Застосунок доступний на http://localhost:8080

### Перше використання

1. Відкрийте http://localhost:8080 у браузері
2. Перетягніть або виберіть фотографії з GPS-даними
3. Натисніть «Завантажити фото»
4. Перейдіть на «Переглянути маршрут», щоб побачити карту

## ✨ Основні можливості

- 🗺️ **Автоматичне побудова маршрутів** з GPS-координат фотографій
- 📷 **Підтримка популярних форматів**: JPEG, HEIC з автоматичним отриманням EXIF
- 🖼️ **Інтерактивна карта** з маркерами та превʼю фотографій
- 🔗 **Шеринг маршрутів** через унікальні посилання
- 📱 **Адаптивний інтерфейс** із drag & drop завантаженням
- 🐳 **Повна контейнеризація** для простого розгортання

## 🏗️ Архітектура

```
┌─────────────────┐    ┌─────────────────┐    ┌─────────────────┐
│   Frontend      │    │    Backend      │    │    Database     │
│                 │    │                 │    │                 │
│ • HTML/CSS/JS   │──▶ │ • FastAPI       │──▶ │ • PostgreSQL    │
│ • Leaflet Maps  │    │ • Python 3.11   │    │ • PostGIS       │
│ • Drag & Drop   │    │ • Async/Await   │    │ • Spatial Data  │
└─────────────────┘    └─────────────────┘    └─────────────────┘
                                │
                                ▼
                       ┌─────────────────┐
                       │ File Processing │
                       │                 │
                       │ • EXIF Parser   │
                       │ • GPS Extract   │
                       │ • Image Convert │
                       └─────────────────┘
```

### Технічний стек

**Backend:**
- **[FastAPI](https://fastapi.tiangolo.com/)** — сучасний async вебфреймворк
- **[SQLAlchemy 2.0](https://sqlalchemy.org/)** — асинхронна ORM
- **[PostgreSQL](https://postgresql.org/) + [PostGIS](https://postgis.net/)** — просторові дані
- **[Pillow](https://pillow.readthedocs.io/) + piexif** — обробка зображень
- **[Alembic](https://alembic.sqlalchemy.org/)** — міграції бази даних

**Frontend:**
- **[Leaflet.js](https://leafletjs.com/)** — інтерактивні карти
- **Vanilla JavaScript** — без додаткових фреймворків
- **Responsive CSS** — адаптивний дизайн

**DevOps:**
- **[Docker](https://docker.com/) + Compose** — контейнеризація
- **[Uvicorn](https://uvicorn.org/)** — ASGI сервер

## 📱 Використання

### 1. Завантаження фотографій

<img src="docs/upload-demo.gif" alt="Завантаження фотографій" width="600">

1. Перейдіть на головну сторінку (http://localhost:8080)
2. Перетягніть фотографії в область завантаження або натисніть для вибору
3. Переконайтеся, що фото містять GPS-дані
4. Натисніть «Завантажити фото»

### 2. Перегляд маршруту

<img src="docs/map-demo.gif" alt="Перегляд маршруту" width="600">

1. Після завантаження натисніть «Переглянути маршрут»
2. На карті відобразяться точки вашого маршруту
3. Клікніть на маркери, щоб переглянути фотографії
4. Використовуйте кнопку «Поділитися маршрутом» для створення публічного посилання

### 3. Керування фотографіями

- **Перегляд усіх фото**: кнопка «Показати фото»
- **Видалення конкретного фото**: хрестик на мініатюрі
- **Очищення всіх фото**: кнопка «Видалити мої фото»

## 🔌 API документація

Після запуску доступна автоматична документація:
- **Swagger UI**: http://localhost:8080/docs
- **ReDoc**: http://localhost:8080/redoc

### Основні ендпойнти

| Метод | Шлях | Опис |
|-------|------|------|
| `POST` | `/api/v1/uploadfiles/` | Завантаження кількох фотографій |
| `GET` | `/api/v1/photos/` | Список фотографій користувача |
| `GET` | `/api/v1/route/` | Отримання маршруту |
| `GET` | `/api/v1/shared_route/{token}` | Публічний доступ до маршруту |
| `DELETE` | `/api/v1/photos/{id}` | Видалення фотографії |

### Приклад API-запиту

```bash
# Завантаження фотографій
curl -X POST "http://localhost:8080/api/v1/uploadfiles/" \
  -H "Content-Type: multipart/form-data" \
  -F "files=@photo1.jpg" \
  -F "files=@photo2.jpg"

# Отримання маршруту
curl -H "Cookie: session_token=your-session-token" \
  "http://localhost:8080/api/v1/route/"
```

## 📁 Структура проєкту

```
photo-route-api/
├── app/                     # Основний застосунок
│   ├── api/v1/             # API ендпойнти
│   │   └── upload.py       # Завантаження й управління фото
│   ├── crud/               # Операції з базою даних
│   │   └── photo.py        # CRUD для фотографій
│   ├── db/                 # Конфігурація БД
│   │   ├── models.py       # Моделі SQLAlchemy
│   │   └── session.py      # Керування сесіями
│   ├── schemas/            # Pydantic-схеми
│   │   └── photo.py        # Схеми для API
│   ├── services/           # Бізнес-логіка
│   │   └── exif_service.py # Обробка EXIF-даних
│   └── main.py             # Точка входу застосунку
├── static/                 # Статичні файли
│   ├── css/               # Стилі
│   └── js/                # JavaScript
├── templates/             # HTML-шаблони
├── uploads/               # Завантажені файли (створюється автоматично)
├── docker-compose.yml     # Конфіг Docker
├── Dockerfile            # Docker-образ
└── requirements.txt      # Python-залежності
```

## ⚙️ Конфігурація

### Змінні середовища (.env)

```bash
# Основні налаштування
DATABASE_URL=postgresql+asyncpg://photo_user:photo-map@db:5432/photo_route

# Додаткові параметри (опційно)
MAX_UPLOAD_SIZE=10485760  # Максимальний розмір файла (10MB)
DEBUG=false               # Режим відладки
CORS_ORIGINS=*            # CORS політика
```


## 🧪 Тестування

### Перевірка працездатності

```bash
# Статус контейнерів
docker-compose ps

# Логи застосунку
docker-compose logs web

# Перевірка API
curl http://localhost:8080/health

# Тест бази даних
docker-compose exec db psql -U photo_user -d photo_route -c "\dt"
```

### Налагодження

```bash
# Перегляд логів у реальному часі
docker-compose logs -f

# Підключення до контейнера
docker-compose exec web bash

# Перевірка змінних середовища
docker-compose exec web env | grep DATABASE
```



## 🌟 Особливості реалізації

### Обробка зображень
- **Асинхронна обробка** EXIF-даних без блокування I/O
- **Підтримка HEIC** з автоконвертацією в JPEG
- **Валідація GPS** координат і часових міток
- **Оптимізація памʼяті** під час роботи з великими файлами

### Геопросторові можливості
- **PostGIS-індекси** для швидкого пошуку за координатами
- **Автоматична маршрутизація** між точками за часом
- **Розрахунок відстаней** на рівні бази даних
- **Spatial queries** для кластеризації близьких точок

### Архітектурні рішення
- **Session-based** автентифікація через cookies
- **Async/await** архітектура для високої продуктивності
- **Repository pattern** для абстракції роботи з даними
- **Dependency injection** через FastAPI

## 📋 Вимоги до фотографій

- **Формат**: JPEG, JPG, HEIC
- **Розмір**: до 10MB на файл
- **GPS-дані**: обовʼязкові (вбудовані в EXIF)
- **Час зйомки**: автоматично зчитується з EXIF

## 🐛 Відомі обмеження

- Фото без GPS-даних ігноруються
- Підтримуються лише сучасні браузери (ES6+)
- Максимум 50 фотографій за одне завантаження
- Авторизація session-based (без постійних акаунтів)

## 📊 Продуктивність

- **Startup time**: ~3 секунди
- **Upload speed**: файл 10MB ~2 секунди
- **Map rendering**: 100 точок ~500ms
- **Database**: підтримка 10k+ фотографій на сесію


---

**Автор**: [Zenzoik](https://github.com/Zenzoik)  
**Live Demo**: [zenzoik.fun](https://zenzoik.fun)  
**Створено**: 2025
